name: claude-swm
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - numpy
  - scipy
  - sympy
  - xarray
  - netcdf4
  - h5netcdf
  - numba
  - pytest
  - pip
  - pip:
    - -e .
